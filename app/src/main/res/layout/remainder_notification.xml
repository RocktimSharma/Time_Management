<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:orientation="vertical">


    <TextView
        android:id="@+id/notification_rem_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:capitalize="words"
        android:text="textView"
        android:textAllCaps="false"
        android:textColor="@color/dodger_blue"
        android:textSize="18sp"
        app:fontFamily="@font/allerta_stencil" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <Button
            android:id="@+id/notification_rem_snooze"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Snooze"
            android:textColor="@color/zeus"
            app:backgroundTint="@color/mercury" />

        <Button
            android:id="@+id/notification_rem_done"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Done"
            android:textColor="@color/zeus"
            app:backgroundTint="@color/mercury" />
    </LinearLayout>
</LinearLayout>